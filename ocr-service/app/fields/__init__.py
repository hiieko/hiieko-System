"""
Field extraction — generic document field extraction from reading-order-aware OCR.

Public API:
  - extract_fields(pages)      — extract fields from reordered pages
  - combine_fields(fields_a, fields_b) — merge field sets (for multi-pass)
"""
